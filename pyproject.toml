[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "multi-agent-orchestrator"
version = "0.1.0"
description = "Autonomous agents that own tasks and coordinate by polling each other for dependency status."
requires-python = ">=3.11"
dependencies = [
    "networkx>=3.0",
]

[project.optional-dependencies]
demo = ["typer>=0.12", "rich>=13.0"]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23"]
ui = ["fastapi>=0.116", "uvicorn>=0.35", "pydantic>=2.0"]
agentic = ["anthropic>=0.97", "pydantic>=2.0"]

[tool.setuptools.packages.find]
include = ["multi_agent_orchestrator*", "demo*", "ui*"]

[tool.setuptools.package-data]
ui = ["static/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
filterwarnings = ["error::RuntimeWarning"]
